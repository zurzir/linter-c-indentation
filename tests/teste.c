#include <stdio.h>
 blas
   df
int main(dfasd
	sdfç ;
)
{
	tipo asd, df ;
	struct a {
		int b;
		int c;
	}  d;
	int a, b,
		d;
	/*
	dfdf*/
	// df
	int r = 0;
	for (1)
		if (2) { //  aq
			for (;;)
				if (0)
					r = 1;
				else if (as)
					r = 2;
				else
					df;
		} // fim
	printf("%d\n", r);
	return 0;

	switch (3) {
		case 3:
		prin();
	}

	if (elemento[0] == '(') {
	//
	} else if (elemento[0] == ')') {
	}
}
