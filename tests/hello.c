if (asdf)
    if (asdf) {
        for () {
            asdf
            x = (
                    (
                    d)
                    )
        }
        if (asdf)
            asdf;
    }
else
    for (asdf) {
        /*
        */
        if (asdf)
            if (asdf)
                // last
                asdf;
    }

switch (3) {
case df:
    df;
}
