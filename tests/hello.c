#include "stdio.h"

int main(int a)
{
    if (a())
    if (b()) ;
    if (a())
    if (b()) ;

    if (bla()) {
    if (ble())
    echo();
    }

    switch (3) {
    case 3:
    dfddd();
    for (;;) ;

    if (3) {
    echo();
    }
    }
}
